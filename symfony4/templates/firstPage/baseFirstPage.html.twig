<!DOCTYPE html>
<html lang="PL">
    <head>
        <meta charset="UTF-8">
        <meta name="Author" content="Mariusz Najwer" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta name="keywords" content="Mariusz Najwer" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1" />
        <link rel="shortcut icon" href="{{ asset('IMG/fav.png')}}">

        <title>{% block title %} {{namePage}} {% endblock %}</title>

        <link href="https://fonts.googleapis.com/css?family=Lato:400,700&amp;subset=latin-ext" rel="stylesheet">
        <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.3.1/css/all.css" integrity="sha384-mzrmE5qonljUremFsqc01SB46JvROS7bZs3IO2EmfFsd15uHvIt+Y8vEf7N7fWAU" crossorigin="anonymous">        
        
        <link href="{{asset('CSS/firstPage/firstPageNav.css') }}" rel="stylesheet" />
       

        {% if footer|default(0) == 1 %}
        <link href="{{asset('CSS/footer.css') }}" rel="stylesheet" />
        {% endif %}

        <style>img[alt*="www.000webhost.com"] {display: none;}</style>
        
        {% block stylesheets %}{% endblock %}
        {% block javascripts %}{% endblock %}

    </head>
    <body>

        <nav class="titleApp">

            {% if nav|default(0) == 0 %}
                <div class="childTitleAppPC">
                    <h1 class="pc">Grupa 1</h1>
                </div>
            {% else %}

                {% if undo|default(0) == 0 %}
                    <div class="childTitleApp">
                        <a class="undo" href="/">
                            <h1><i class="fas fa-undo-alt"></i></h1>
                        </a>
                    </div>
                {% endif %}

                {% if undo|default(0) == 1 %}
                     <div class="childTitleApp">
                        <a class="undo" href="/user/login">
                            <h1><i class="fas fa-undo-alt"></i></h1>
                        </a>
                    </div>
                {% endif %}

            {% endif %}

            <div class="childTitleApp">
                <h1><i class="fas fa-heartbeat"></i> KAM</h1>
            </div>

            <div class="childTitleAppPC">
                <ul class="social">
                    Politechnika Wrocławska
                </ul>
            </div>
        </nav>

        {% block body %}{% endblock %}

        {% if footer|default(0) == 1 %}
            <div class="push"></div>
            <footer>
                <div class="laws">
                    <p><i class="fas fa-gavel"></i> Wszystkie&nbsp;prawa&nbsp;zastrzeżone</p>
                    <p><i class="far fa-copyright"></i> PWr 2019 </p>
                    <p> <i class="fas fa-cookie-bite"></i> Cookie</p>
                </div>
                <div class="endline"></div>
            </footer>
        {% endif %}

        
    </body>

   

</html>
